import 'package:flutter/widgets.dart';

class FavoriesTab extends StatelessWidget {
  const FavoriesTab({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Favoties')
    );
  }
}